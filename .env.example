# --- Postgres (obbligatorio) ---
# Su Vercel+Neon queste variabili sono già create dall’integrazione.
# In locale: DATABASE_URL (pooler) e DATABASE_URL_UNPOOLED (host diretto); se non usi il pooler, stessa stringa per entrambe.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.region.aws.neon.tech/neondb?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST.region.aws.neon.tech/neondb?sslmode=require"

# --- Auth.js / NextAuth ---
AUTH_SECRET="genera-con-openssl-rand-base64-32"
# In produzione: dominio canonico (es. https://convivia.it). Evita *.vercel.app se usi dominio custom.
AUTH_URL="http://localhost:3000"

# --- Email transazionali (Resend) — verifica account, reset password, inviti casa, trasferimento proprietà ---
# https://resend.com — in dev senza chiave i link vengono solo loggati in console.
RESEND_API_KEY=""
EMAIL_FROM="Convivia <onboarding@resend.dev>"
# Produzione: stesso dominio canonico delle email (es. https://convivia.it). Se omesso, si usa AUTH_URL poi VERCEL_URL.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Web Push (notifiche PWA) ---
# In produzione su Vercel: `VAPID_PRIVATE_KEY` (solo dashboard, mai in git). La chiave pubblica e VAPID_SUBJECT
# sono anche in `vercel.json` per il progetto Convivia. In locale copia in `.env.local`:
#   npx --yes web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:notify@convivia.app"
